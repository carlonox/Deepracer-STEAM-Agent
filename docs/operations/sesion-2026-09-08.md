# Sesión 2026-09-08 — Tandem aula (PC U + robot amss-wuot)

Fase 1 física + resurrección post-reboot. Operador presente, zona despejada,
todo por backend :5002. Robot elevado para pruebas salvo indicación.

## Red (Tailscale gana)
- PC y robot en WiFi `NachoNacho`, pero el DHCP movió al robot `.103` → `.106`.
- Robot en tailnet como `amss-wuot`: `100.117.192.31` estable (SSH/API/cámara OK).
- Decisión: `DEEPRACER_HOST=100.117.192.31`, `DEEPRACER_API_PORT=443`.
- Hallazgo backend: `vehicleControl.js` habla HTTPS fijo; el `:5001` directo
  sirve HTTP plano (TLS solo en nginx `:443`). Con `443` la sesión va (`Sesión lista`).

## Reloj congelado
- Al arrancar sin internet el reloj queda viejo y Tailscale/SSH mueren (mTLS).
- Fix aplicado: `timedatectl set-ntp true` (2.º intento autenticó) + reboot.
- `@reboot`+`ntpdate` del checklist no basta si no hay red al bootear.

## Cámara sin frames → OK tras reboot
- `web_video_server` vivo pero snapshot/stream vacíos; `dmesg`: `uvcvideo: Buffer is NULL` en loop.
- USB físicamente bien. `sudo reboot` lo resolvió (snapshot 200 ~10KB).

## Hardware: cable motor-ruedas suelto
- Síntoma: API 200 en todo, motor sonaba desde +0.20 pero sin girar.
- Causa: cable motor↔ruedas desconectado. Re-conectado por operador.

## Barrido de throttle (elevado, ráfagas 1-1.5 s, ~10-23 cmds/s, stop tras cada una)
| Throttle norm. | Real aprox. | Resultado |
|---|---|---|
| +0.10 | 0.55 | nada |
| +0.15 | 0.575 | nada |
| +0.20 | 0.60 | nada en suelo; elevado flojo luego OK tras cable |
| +0.30 | 0.65 | motor suena, no gira (previo al cable) |
| +0.40 | 0.70 | adelante OK (ver abajo) |
| +0.50 atrás | 0.75 | nada |
| +0.55 atrás | 0.775 | gira muy poquito, sin energía |
| +0.60 atrás | 0.80 | se mueve bien |
| -0.40 adelante | -0.70 | se mueve bien |
| -0.60 adelante | -0.80 | se mueve más |

## Convención de este boot
**Negativo = adelante, positivo = atrás** (igual que 2026-07-31).
Asimetría nueva: adelante mueve desde ~0.40-norm; reversa exige ~0.55-0.60-norm
(real ~0.78-0.80). Hipótesis: batería baja agranda el dead-zone + posible
calibración ESC asimétrica. Verificar con LiPo llena y medir voltaje por I2C.

## Pendiente
- Repetir en suelo con LiPo llena (hoy no desplazó ni con 0.65-real).
- Device token (`/opt/aws/deepracer/token.txt`): solo lectura, rotación pendiente.
- Prueba de steering (trim) cuando haya tracción en suelo.
