# Sesión 2026-09-10 — VR/AR, revisor propio, vault de secretos y Hermes

Continuación del tándem del 2026-09-08. PC Windows (`C:\Users\UNAL\Deepracer-STEAM-Agent`).
El robot quedó **apagado** al cierre (no hubo pruebas de movimiento).

## Frontend / Quest (PR #18, mergeado)

- `fix/vr-triggers` (`0832de1`): VR con estado **WebXR real** (RT adelante, LT
  freno/reversa, stick gira) en vez del hack de rueda de mouse; selector
  **Joystick/Gatillos** en Manual y VR; **freno arcade** (Grip/A); **stop
  imparable** (salta el throttle, reintentos) + **dead-man**; render XR mínimo
  (cámara + HUD, letterbox) y **modo AR** con passthrough; fallback Gamepad API.
- **HTTPS + proxy opt-in** para Quest por LAN (`VITE_API_PROXY=1` + certs);
  documentado en `apps/frontend/README.md`.
- Trim del servo probado en vivo (varios valores): no monotónico por banda
  muerta/ruido; **parked en 0**. Pendiente validar en pista con LiPo llena.

## Revisor propio (CI)

- **#24** (`fix/reviewer-timeout`): `retry_same_model_on_timeout=false`,
  `ai_timeout=90`, `timeout-minutes: 8` — evita cuelgues de ~16 min.
- **#26** (`fix/reviewer-reasoning`): `reasoning_effort: low` (nex-pro pensaba
  ~5.6k tokens y rozaba el tope de 8 min).
- Ambos mergeados.

## Docs (PR #23, abierto)

- README frontend: el proxy exige **cert HTTPS válido**.
- Plan: ESP32 reconciliado (descartado como sensor; teleop BLE como **diseño no
  adoptado**, sujeto a la regla 1 de control físico).

## Vault local de secretos (PR #25, abierto)

- Modelo **una identidad `age` por persona**: llave privada cifrada con su PIN
  en su USB (`identity.age`); el vault `secrets.age` se cifra a la **unión de
  llaves públicas** (`recipients.txt`).
- `scripts/vault/`: `vault-common`, `init`, `unlock`, `lock`, `watch`,
  `install-watcher`, `add-recipient`, `new-identity`, `new-recovery`, `recover`,
  `show-secret`, `DeepRacerVaultGui.ps1` (GUI WinForms) y `build-gui-exe.ps1`.
- **Llave de recuperación** con escrow en BWS (2FA) → `recover-vault.ps1`.
- GUI local (tema oscuro, **LEDs** USB/Vault/backend/Hermes, visor de logs) y
  **.exe** con ps2exe (el binario no se versiona).
- `age` 1.3.1 instalado por winget.
- **Validado**: descifrado con PIN, PIN incorrecto rechazado, backend arrancado
  con secretos inyectados, recuperación con la llave de BWS.

## Hermes

- **Secretos migrados al vault**: el `.env` de la raíz quedó **sin credenciales**
  (solo config); el vault guarda los 8 secretos. Hermes los recibe por `${VAR}`
  en `docker-compose.yml` (arrancar desde consola desbloqueada).
- La imagen `:latest` cambió la **auth del dashboard**: exige
  `dashboard.basic_auth.username` + `password_hash` en `hermes/config.yaml`
  (ya no se usa la env). Aplicado; dashboard `READY` en `:9119` (host `:9999`).
- `reasoning_effort: ultra` ya no existe → `medium`. Nota: DeepSeek vía
  `opencode-go` no pasa `reasoning_effort` (issue #21577 upstream).
- **SOUL**: Hermes lee `HERMES_HOME/SOUL.md` (`/opt/data/SOUL.md`), no
  `soul/soul.md`; el compose monta `hermes/soul/soul.md` **read-only** sobre esa
  ruta. `hermes/SOUL.md` (runtime) pasa a `.gitignore`. Verificado: SpeedRacer.

## Estado de PRs

- Mergeados: **#18**, **#24**, **#26**.
- Abiertos: **#23** (docs), **#25** (vault) — ambos actualizados con `main`.

## Pendientes priorizados

1. Mergear **#23** y luego **#25** (re-sincronizar si GitHub lo pide).
2. Probar VR triggers en el casco + trim en pista con LiPo llena.
3. Recrear Hermes desde consola desbloqueada para activar el mount del SOUL.
4. Rotación de credenciales del robot (robot encendido + SSH, con autorización).
5. Colaboradores en GitHub (fin del bypass) y revisar secretos en
   `hermes/config.yaml` / `auth.json` (aparte del vault).
6. (Fase 4/1.5) RAG deps + Ollama; backup/upgrade de Hermes.

## Entorno

- Hermes **v0.18.0 (2026.7.1)**, upstream `009b42d0`, Python 3.13.5,
  imagen `nousresearch/hermes-agent@sha256:1fac25f7…`.
- Docker Desktop en Windows; `age` 1.3.1; PowerShell 5.1 / 7.6.5.
