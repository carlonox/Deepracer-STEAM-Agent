# Herramientas binarias

| Archivo | Propósito |
|---|---|
| `arduino-cli` | CLI local para compilar o cargar firmware Arduino. |

Los binarios deben documentar versión, origen y hash. La estructura objetivo
renombra esta carpeta a `tools/` para distinguirla del código de aplicación.
